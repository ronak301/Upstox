import { View, Text } from "react-native";
import React from "react";
import HoldingsScreen from "../src/modules/holdings/screens/HoldingsScreen";

const StartPage = () => {
  return <HoldingsScreen />;
};

export default StartPage;
